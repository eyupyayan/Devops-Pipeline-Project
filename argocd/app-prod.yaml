apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/DEG/task-api-helm.git
    targetRevision: master
    path: Devops-Pipeline-Project/argocd
    helm:
      valueFiles:
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true